pipeline {
    agent any
	environment{
	NEW_VERSION = '1.3.0'
	}
    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout'
				echo "Building Verison ${NEW_VERSION}"
            }
        }
		}
		}
